
# Module is collection of functions, classes and variables if we want to use this collection we need to import module in are program. There are inbuilt and user define modules
#1. Inbuilt modules:
    # modules which are already define by python is called inbuilt module.
    # Example: math.
    # Using it: 
import math
print(math.sqrt(4))
print(math.floor(2.4))
print(math.tanh(2))
print(math.pi)
print(math.pow(2,4))
